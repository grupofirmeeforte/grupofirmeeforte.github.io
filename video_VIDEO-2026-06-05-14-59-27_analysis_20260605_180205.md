Com base no vídeo, aqui está uma descrição detalhada do que está acontecendo e dos problemas encontrados:

**Contexto Geral:**
O usuário está tentando fazer o upload de um arquivo PDF para um sistema web (aparentemente um sistema de gestão de contratos). A página possui uma área que diz "Arraste os PDFs aqui ou clique para selecionar".

**Problema 1: Arquivos invisíveis na janela de seleção do navegador**
1. O usuário clica na área de upload, o que abre a janela padrão do Windows para "Abrir" (selecionar) um arquivo.
2. Inicialmente, a janela mostra a pasta "Desktop" (Área de Trabalho), onde alguns arquivos PDF são visíveis.
3. O usuário navega para a pasta "Downloads" pelo menu lateral esquerdo.
4. **Comportamento Inesperado:** A pasta "Downloads" aparece completamente vazia na janela de seleção do navegador, exibindo a mensagem "Nenhum item corresponde à pesquisa". O usuário comenta: "Tá vendo? Não aparece nada."

**Problema 2: Falha ao arrastar e soltar (Drag and Drop)**
1. O usuário fecha a janela de seleção e abre uma janela separada do Explorador de Arquivos do Windows, também na pasta "Downloads".
2. Nesta janela do Windows, é possível ver vários arquivos. No entanto, se você observar de perto, os arquivos têm a extensão **`.pdf.crdownload`**. Isso indica que são arquivos cujo download pelo Google Chrome foi interrompido ou ainda não foi concluído; eles não são arquivos PDF finalizados.
3. O usuário seleciona um desses arquivos (`Proposta-210670668.pdf.crdownload`) e o arrasta para a área de upload no navegador.
4. **Erro:** O sistema rejeita o arquivo. Um pequeno aviso pop-up aparece no canto inferior direito dizendo **"Nenhum PDF selecionado"**. O usuário comenta: "Ele fala que não deu certo." O sistema provavelmente espera um arquivo com a extensão `.pdf` válida e rejeita o formato `.crdownload`.

**Repetição do Problema 1:**
1. O usuário tenta novamente o primeiro método, clicando na área de upload para abrir a janela de seleção.
2. Ele navega novamente para a pasta "Downloads".
3. Mais uma vez, a pasta aparece vazia dentro da janela de seleção do navegador ("Nenhum item corresponde à pesquisa"), confirmando o problema inicial de que o navegador não está conseguindo "ver" os arquivos que estão lá (provavelmente porque a janela está filtrando apenas por arquivos `.pdf` válidos, e os arquivos na pasta são `.crdownload`).

**Resumo:**
O usuário está enfrentando dois problemas interligados:
1. Ele está tentando fazer upload de arquivos que não terminaram de ser baixados (extensão `.crdownload`).
2. O sistema web rejeita esses arquivos incompletos quando arrastados, e a janela de seleção de arquivos do navegador os oculta completamente, pois provavelmente está configurada para mostrar apenas arquivos PDF válidos.